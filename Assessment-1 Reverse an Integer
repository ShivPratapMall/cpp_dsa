#include<iostream>
using namespace std;
int main()
{
    int n,i,sum=0,rem;
    cout<<"Enter a number: ";
    cin>>n;
    for(i=n; i>0; i=i/10)
    {
        rem=i%10 ;
        sum = rem + (sum*10);
      
    }
      cout<<sum;
    return 0;
}
