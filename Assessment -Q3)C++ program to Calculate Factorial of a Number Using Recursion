#include<iostream>
using namespace std;
int fact(int );
int main()
{
    int n,res;
    cout<<"Enter a number ";
    cin>>n;
    res = fact(n);
    cout<<res;
}
int fact (int n)
{
    if (n==0)
    {
        return 1;
    }
    else
    {
        return(n*fact(n-1));
    }
}
